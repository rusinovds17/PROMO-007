# Конфигурация с секретными данными
config.py
.env
.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Виртуальные окружения
venv/
ENV/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Данные и логи
*.log
*.json.bak
backup/

# Секреты - вся папка secrets/ (кроме README.md)
secrets/*
!secrets/README.md
# Но явно указываем основные файлы для ясности
secrets/user_data/
secrets/vk/
secrets/*.key
secrets/*.txt
secrets/*.json
secrets/PASSWORD.txt
*.key
# Старые файлы в корне (если остались после переноса)
PASSWORD.txt
counters.json
metrics.json
vk_ord_tokens.json
vk_ord_state.json

# Старые файлы с хардкод токенами (не используются, содержат секреты)
ОРДГОТОВНА99.py

# OS
.DS_Store
Thumbs.db

# Docker (не игнорируем .dockerignore, он нужен в репозитории)

