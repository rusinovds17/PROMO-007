# 🚀 Руководство по настройке для нового инженера

## Первоначальная настройка проекта

### 1. Клонирование репозитория

```bash
git clone https://github.com/AllVolLOAD/Promo_since.git
cd Promo_since
```

### 2. Настройка окружения

#### Установка Python зависимостей
```bash
pip install -r requirements.txt
```

**Требуемые версии:**
- Python 3.11+
- aiogram 3.12.0
- python-docx 1.1.2
- aiohttp >= 3.9.0
- cryptography >= 41.0.0

#### Установка Docker (опционально)
Если планируется использовать Docker:
```bash
# Скачать Docker Desktop с https://www.docker.com/products/docker-desktop/
```

### 3. Конфигурация

#### Создание config.py
```bash
cp config.example.py config.py
```

#### Заполнение config.py
Откройте `config.py` и укажите:

1. **BOT_TOKEN** - токен вашего Telegram бота
   - Получить: https://t.me/BotFather
   - Команда: `/newbot`

2. **VK_ORD_API_TOKEN** - токен VK.ОРД API (опционально)
   - Получить: https://ord.vk.com

3. **ADMIN_CHAT_ID** - ID группы для метрик
   - Получить через бота: https://t.me/userinfobot

4. **VK_ORD_API_BASE** - URL API (sandbox или production)
   - Sandbox: `https://api-sandbox.ord.vk.com`
   - Production: `https://api.ord.vk.com`

### 4. Создание необходимых директорий и файлов

```bash
# Создать структуру secrets/
mkdir -p secrets/user_data

# Создать пустые JSON файлы (будут заполнены автоматически при первом запуске)
echo "{}" > secrets/counters.json
echo '{"unique_users": {}, "total_count": 0, "daily_registrations": {}}' > secrets/metrics.json
echo "{}" > secrets/vk_ord_tokens.json
echo "{}" > secrets/vk_ord_state.json
```

### 5. Проверка шаблонов

Убедитесь, что все шаблоны на месте:
```bash
ls templates/
# Должны быть:
# - schet-oferta.docx
# - schet-oferta2-multi.docx
# - schet-oferta2-multiPRO.docx
# - dogovor_rim.docx
# - dogovor_rim2-multi.docx
```

### 6. Первый запуск

#### Вариант 1: Прямой запуск
```bash
python ПОЛИРОЛЬОТБИВКИ.py
```

**Ожидаемый вывод:**
```
Запускаю бота @your_bot_name (id=123456789)
```

#### Вариант 2: Docker
```bash
docker-compose build
docker-compose up -d
docker-compose logs -f
```

### 7. Тестирование

1. Найдите бота в Telegram: `@your_bot_name`
2. Отправьте `/start`
3. Проверьте главное меню
4. Попробуйте создать счет (тестовый)

### 8. Проверка метрик

Отправьте боту команду `/stats` - должна прийти статистика в указанную группу.

---

## Структура после настройки

```
Promo_since/
├── config.py                    ✅ (создан и заполнен)
├── secrets/
│   ├── counters.json            ✅ (создан, пустой)
│   ├── metrics.json             ✅ (создан, пустой)
│   ├── vk_ord_tokens.json       ✅ (создан, пустой)
│   ├── vk_ord_state.json        ✅ (создан, пустой)
│   └── user_data/               ✅ (создан)
└── templates/                   ✅ (проверен)
```

---

## Частые проблемы

### Проблема: "config.py не найден"
**Решение:** Создайте из примера: `cp config.example.py config.py`

### Проблема: "BOT_TOKEN не задан"
**Решение:** Заполните `BOT_TOKEN` в `config.py`

### Проблема: "Module not found: aiogram"
**Решение:** Установите зависимости: `pip install -r requirements.txt`

### Проблема: "Template not found"
**Решение:** Проверьте наличие файлов в `templates/`

### Проблема: "Permission denied" при создании файлов
**Решение:** Проверьте права доступа к папке `secrets/`

---

## Дополнительные ресурсы

- [PROJECT_DOCUMENTATION.md](PROJECT_DOCUMENTATION.md) - Полная документация
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура проекта
- [README.md](README.md) - Базовая документация

---

**Готово к работе!** 🎉

